<div class="container">
  <div class="page-header">
    <h1>Buat akun baru <small>Ayo Gabung Sekarang Dengan Minang Tourism FREE!</small></h1>
  </div>

  <div class="row">
    <div class="span8">
      <%= simple_form_for(resource, :as => resource_name,
        :url => registration_path(resource_name),
        :html => {:class => 'form-horizontal'}) do |f| %>
        <%= f.error_notification %>
        <div class="inputs">
          <%= f.input :username, :label => 'Username', :required => true%>
          <%= f.input :email, :label => 'Alamat Email',:required => true, :autofocus => true %>
          <%= f.input :password, :label => 'Kata Sandi', :required => true %>
          <%= f.input :password_confirmation, :label => 'Ulangi Kata Sandi', :required => true %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "Register", :class => 'btn btn-primary' %>
          <p class="help-block">Already registered? <%= render :partial => "devise/shared/links" %></p>
        </div>
      <% end %>

    </div>
  </div>
</div>