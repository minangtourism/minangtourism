<%= simple_form_for @category_loc_tourism, :html => { :class => 'form-horizontal' } do |f| %>

  <%= f.input :name %>

  <%#= f.select :parent_id, nested_set_options(CategoryLocTourism, @category_loc_tourism) {|i, level| "#{'-' * level} #{i.name}" } %>
  <%= f.association :parent, :collection => nested_set_options(CategoryLocTourism.all.reject{|i| i == f.object}) {|i, level| "#{'-' * level} #{i.name}" } %>

  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
      category_loc_tourisms_path, :class => 'btn' %>
  </div>
<% end %>
