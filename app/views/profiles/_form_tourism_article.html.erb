<%= f.input :title %>
<%= f.input :content %>
<%= f.input :image %>
<div class="form-actions">
  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
    tourism_articles_profile_path(@profile), :class => 'btn btn-primary' %>
</div>

